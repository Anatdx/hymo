name: Build Hymo

on:
  push:
    branches:
      - main
      - dev
      - lkm
    paths-ignore:
      - '**.md'
      - '.gitignore'
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      hymofs_ref:
        description: 'HymoFS ref to clone (branch/tag/SHA)'
        required: false
        default: 'lkm'
      hymofs_sha:
        description: 'HymoFS commit SHA (for TG caption when triggered by HymoFS CI)'
        required: false
      hymofs_commit_message:
        description: 'HymoFS commit message (for TG caption)'
        required: false
      hymofs_run_id:
        description: 'HymoFS workflow run ID (to download LKM artifacts)'
        required: false
  # 由 HymoFS 仓库 call 触发：构建完整模块（与 push 时相同）
  # 注意：repository_dispatch 只有默认分支上的 workflow 会被触发
  repository_dispatch:
    types: [build-lkm]

jobs:
  build-lkm:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch' || github.event_name == 'pull_request' || (github.event_name == 'push' && !contains(github.event.head_commit.message, '[skip ci]'))
    strategy:
      fail-fast: false
      matrix:
        kmi:
          - android12-5.10
          - android13-5.10
          - android13-5.15
          - android14-5.15
          - android14-6.1
          - android15-6.6
          - android16-6.12
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build HymoFS LKM for ${{ matrix.kmi }}
        env:
          HYMOFS_REPO: Anatdx/HymoFS
          HYMOFS_REF: ${{ github.event.inputs.hymofs_ref || github.event.client_payload.ref || 'lkm' }}
        run: |
          set -euo pipefail
          KMI="${{ matrix.kmi }}"
          echo "Cloning HymoFS (ref: ${HYMOFS_REF})..."
          git clone --depth 1 "https://github.com/${HYMOFS_REPO}.git" hymofs_src
          (cd hymofs_src && git fetch --depth 1 origin "${HYMOFS_REF}" && git checkout FETCH_HEAD)
          echo "Cloning kernel tree (${KMI})..."
          git clone --depth 1 -b "${KMI}" https://android.googlesource.com/kernel/common kernel_src
          sudo apt-get update
          sudo apt-get install -y git make clang lld llvm gcc gcc-aarch64-linux-gnu flex bison bc rsync cpio libelf-dev libssl-dev libdw-dev pahole zip
          # 5.10: ubuntu clang has arm64 "sp" register issues; use GCC cross-compiler
          if [[ "$KMI" == android12-5.10 ]] || [[ "$KMI" == android13-5.10 ]]; then
            LLVM_ARGS=""
            CROSS_ARGS="CROSS_COMPILE=aarch64-linux-gnu-"
          else
            LLVM_ARGS="LLVM=1 LLVM_IAS=1"
            CROSS_ARGS=""
          fi
          make -C kernel_src ARCH=arm64 $CROSS_ARGS $LLVM_ARGS gki_defconfig 2>/dev/null || \
            make -C kernel_src ARCH=arm64 $CROSS_ARGS $LLVM_ARGS defconfig
          # 6.12: disable gendwarfksyms to avoid dwarf.h dependency
          if [[ "$KMI" == *"6.12"* ]] && [ -f kernel_src/scripts/config ]; then
            kernel_src/scripts/config --file kernel_src/.config -d GENDWARFKSYMS 2>/dev/null || true
            make -C kernel_src ARCH=arm64 $CROSS_ARGS $LLVM_ARGS olddefconfig 2>/dev/null || true
          fi
          make -C kernel_src ARCH=arm64 $CROSS_ARGS $LLVM_ARGS modules_prepare
          make -C kernel_src ARCH=arm64 $CROSS_ARGS $LLVM_ARGS M="$GITHUB_WORKSPACE/hymofs_src/src" KBUILD_MODPOST_WARN=1 modules
          mkdir -p out
          cp hymofs_src/src/hymofs_lkm.ko "out/${KMI}_hymofs_lkm.ko"
          llvm-strip -d "out/${KMI}_hymofs_lkm.ko" 2>/dev/null || true
          ls -la out/

      - name: Upload LKM artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.kmi }}_hymofs_lkm.ko
          path: out/${{ matrix.kmi }}_hymofs_lkm.ko

  build:
    needs: build-lkm
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch' || github.event_name == 'pull_request' || (github.event_name == 'push' && !contains(github.event.head_commit.message, '[skip ci]'))

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Download LKM artifacts
        uses: actions/download-artifact@v4

      - name: Prepare module/lkm folder
        run: |
          mkdir -p module/lkm
          find . -name "*.ko" -path "*hymofs_lkm*" -exec cp {} module/lkm/ \;
          ls -la module/lkm/

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c
        id: setup-ndk

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: webui/package-lock.json

      - name: Install WebUI Dependencies
        run: |
          cd webui
          npm install

      - name: Setup CMake and Ninja
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build

      - name: Set version (tag = Version Name, versionCode = Commit Count)
        run: |
          COMMIT_COUNT=$(git rev-list --count HEAD)
          SHORT_HASH=$(git rev-parse --short=8 HEAD)
          VERSION_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "VERSION_TAG=$VERSION_TAG" >> $GITHUB_ENV
          echo "SHORT_HASH=$SHORT_HASH" >> $GITHUB_ENV
          echo "COMMIT_COUNT=$COMMIT_COUNT" >> $GITHUB_ENV
          echo "MODULE_VERSION=$VERSION_TAG" >> $GITHUB_ENV
          sed -i "s/^version=.*/version=${VERSION_TAG}/" module/module.prop
          sed -i "s/^versionCode=.*/versionCode=${COMMIT_COUNT}/" module/module.prop
          echo "Building version: $VERSION_TAG (versionCode=$COMMIT_COUNT), zip: hymo-${VERSION_TAG}-${SHORT_HASH}.zip"

      - name: Build Project
        env:
          ANDROID_NDK: ${{ steps.setup-ndk.outputs.ndk-path }}
          NDK_PATH: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          echo "NDK Path: $ANDROID_NDK"
          ./build.sh init
          ./build.sh package

      - name: Rename to hymo-vx.x.x-xxxxxxxx.zip
        run: |
          cd build/out
          TARGET="hymo-${VERSION_TAG}-${SHORT_HASH}.zip"
          for file in *.zip; do
            [ -f "$file" ] && [ "$file" != "$TARGET" ] && mv "$file" "$TARGET"
            break
          done
          ls -la

      - name: Sign Module (Internal)
        if: github.actor == 'Anatdx'
        env:
          MODULE_PRIVATE_KEY: ${{ secrets.MODULE_PRIVATE_KEY }}
        run: |
          if [ -z "$MODULE_PRIVATE_KEY" ]; then
            echo "⚠️ No MODULE_PRIVATE_KEY secret found. Skipping internal signing."
          else
            echo "🔐 Found private key, performing internal signing..."
            
            # Install dependencies
            pip3 install cryptography
            
            # Write private key
            echo "$MODULE_PRIVATE_KEY" > private.pem
            
            # Generate Certificate (Self-signed) from Private Key
            openssl req -new -x509 -key private.pem -out certificate.pem -days 10000 \
              -subj "/C=CN/ST=Hymo/L=Hymo/O=Hymo/OU=Hymo/CN=Hymo Module"
              
            # Locate Zip
            ZIP_FILE=$(ls build/out/*.zip | head -n 1)
            echo "Signing $ZIP_FILE..."
            
            # Run signing script
            python3 script/sign_zip.py "$ZIP_FILE" "build/out/signed.zip" private.pem certificate.pem
            
            # Replace original with signed
            mv "build/out/signed.zip" "$ZIP_FILE"
            
            # Cleanup
            rm private.pem certificate.pem
            echo "✅ Internal signing complete."
          fi

      # 手动导入 GPG，避免 crazy-max/ghaction-import-gpg 的 keygrip preset 在 runner 上报 Not implemented
      - name: Import GPG key
        if: github.actor == 'Anatdx'
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        run: |
          gpg --batch --import <<< "$GPG_PRIVATE_KEY"
          gpg --list-secret-keys --keyid-format long
      
      - name: Sign Module with GPG
        if: github.actor == 'Anatdx'
        env:
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          cd build/out
          ZIP_FILE=$(ls *.zip | head -n 1)
          echo "Signing $ZIP_FILE..."
          gpg --detach-sign --local-user an@anatdx.com --batch --yes --pinentry-mode loopback \
            --passphrase "$GPG_PASSPHRASE" \
            --output "$ZIP_FILE.asc" "$ZIP_FILE"
          if [ -f "$ZIP_FILE.asc" ]; then
            echo "Signature created successfully:"
            ls -lh "$ZIP_FILE.asc"
          else
            echo "Error: Signature file not created!"
            exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Hymo-${{ env.MODULE_VERSION }}
          path: |
            build/out/*.zip
            build/out/*.zip.asc
          if-no-files-found: error

      - name: "📤 Upload to telegram | 上传至Telegram群组"
        run: |
          if [ -z "$BOT_TOKEN" ] || [ -z "$CHAT_ID" ]; then
            echo "BOT_TOKEN/CHAT_ID not set, skipping."
            exit 0
          fi
          echo "📤 正在上传到Telegram..."
          pip3 install telethon
          python3 ${{ github.workspace }}/script/hymobot.py build/out/*.zip
          echo "✅ 文件已上传到Telegram"
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          MESSAGE_THREAD_ID: ${{ secrets.MESSAGE_THREAD_ID }}
          # 由 HymoFS CI 触发时展示 HymoFS 的 commit，否则展示 hymo 的
          COMMIT_URL: ${{ github.event.inputs.hymofs_sha && format('{0}/Anatdx/HymoFS/commit/{1}', github.server_url, github.event.inputs.hymofs_sha) || format('{0}/{1}/commit/{2}', github.server_url, github.repository, github.sha) }}
          COMMIT_MESSAGE: ${{ github.event.inputs.hymofs_commit_message || github.event.head_commit.message }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TITLE: "Hymo Build"
          VERSION: ${{ env.MODULE_VERSION }}
          BRANCH: ${{ github.ref_name }} 
